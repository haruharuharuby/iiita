%ul.list-inline.tag-list
  = content_tag_for :li, tags do |tag|
    %small
      .associated-tag
        = link_to tag do
          %span.glyphicon.glyphicon-tags #{ tag.name }
          = "|#{tag.articles_count}" if count_on
